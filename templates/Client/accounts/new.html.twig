{% extends 'base.html.twig' %}

{% block title %}Ouvrir un Nouveau Compte • Banking System{% endblock %}

{% block body %}
<div class="container py-5">
    <nav aria-label="breadcrumb" class="mb-4">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="{{ path('client_accounts') }}">Mes Comptes</a></li>
            <li class="breadcrumb-item active">Nouveau Compte</li>
        </ol>
    </nav>

    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow-sm">
                <div class="card-header bg-primary text-white">
                    <h4 class="mb-0"><i class="fas fa-plus-circle me-2"></i>Ouvrir un Nouveau Compte</h4>
                </div>
                <div class="card-body">
                    {{ form_start(form) }}
                    
                    <div class="mb-4">
                        <h5 class="text-primary"><i class="fas fa-info-circle me-2"></i>Informations</h5>
                        <p class="text-muted">Remplissez ce formulaire pour ouvrir un nouveau compte bancaire.</p>
                    </div>
                    
                    <div class="mb-3">
                        {{ form_label(form.type) }}
                        {{ form_widget(form.type, {'attr': {'class': 'form-select'}}) }}
                        {{ form_errors(form.type) }}
                    </div>
                    
                    <div class="mb-3">
                        {{ form_label(form.initialDeposit) }}
                        <div class="input-group">
                            {{ form_widget(form.initialDeposit, {'attr': {'class': 'form-control'}}) }}
                            <span class="input-group-text">€</span>
                        </div>
                        {{ form_errors(form.initialDeposit) }}
                        <div class="form-text">Minimum 50€. Ce montant sera crédité immédiatement sur votre compte.</div>
                    </div>
                    
                    <div class="mb-4">
                        {{ form_label(form.purpose) }}
                        {{ form_widget(form.purpose, {'attr': {'class': 'form-control', 'rows': 3}}) }}
                        {{ form_errors(form.purpose) }}
                    </div>
                    
                    <div class="card mb-4">
                        <div class="card-body bg-light">
                            <h6><i class="fas fa-file-contract me-2"></i>Conditions générales</h6>
                            <ul class="small text-muted mb-0">
                                <li>Le compte sera activé immédiatement après validation</li>
                                <li>Découvert autorisé uniquement sur les comptes courants</li>
                                <li>Frais de tenue de compte selon le type choisi</li>
                                <li>Votre identité a déjà été vérifiée lors de votre inscription</li>
                            </ul>
                        </div>
                    </div>
                    
                    <div class="d-grid gap-2">
                        <button type="submit" class="btn btn-primary btn-lg">
                            <i class="fas fa-check me-2"></i>Ouvrir le compte
                        </button>
                        <a href="{{ path('client_accounts') }}" class="btn btn-outline-secondary">
                            <i class="fas fa-times me-2"></i>Annuler
                        </a>
                    </div>
                    
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}