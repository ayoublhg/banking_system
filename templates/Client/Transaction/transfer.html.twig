{% extends 'base.html.twig' %}

{% block title %}Virement • Banking System{% endblock %}

{% block body %}
<div class="container py-5">
    <nav aria-label="breadcrumb" class="mb-4">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="{{ path('client_accounts') }}">Mes Comptes</a></li>
            <li class="breadcrumb-item"><a href="{{ path('client_transactions_index') }}">Transactions</a></li>
            <li class="breadcrumb-item active">Virement</li>
        </ol>
    </nav>

    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            <div class="card shadow-sm">
                <div class="card-header bg-info text-white">
                    <h4 class="mb-0"><i class="fas fa-exchange-alt me-2"></i>Effectuer un Virement</h4>
                </div>
                <div class="card-body">
                    {% if accounts|length == 0 %}
                        <div class="alert alert-warning">
                            <i class="fas fa-exclamation-triangle me-2"></i>
                            Vous n'avez aucun compte actif. Contactez votre conseiller.
                        </div>
                    {% else %}
                        <form method="POST" action="{{ path('client_transaction_transfer') }}">
                            <div class="mb-3">
                                <label for="from_account" class="form-label">Compte source *</label>
                                <select class="form-control" id="from_account" name="from_account" required>
                                    <option value="">Sélectionnez votre compte</option>
                                    {% for account in accounts %}
                                        <option value="{{ account.id }}">
                                            {{ account.getTypeLabel() }} - {{ account.accountNumber }}
                                            (Solde: {{ account.balance|number_format(2, ',', ' ') }} €)
                                        </option>
                                    {% endfor %}
                                </select>
                            </div>
                            
                            <div class="mb-3">
                                <label for="to_account" class="form-label">Compte destination *</label>
                                <input type="text" class="form-control" id="to_account" name="to_account" 
                                       required placeholder="Ex: FR7612345678901234567890123"
                                       pattern="[A-Z]{2}[0-9]{2}[A-Z0-9]{1,30}">
                                <div class="form-text">Format IBAN: 2 lettres + 2 chiffres + jusqu'à 30 caractères</div>
                            </div>
                            
                            <div class="mb-3">
                                <label for="amount" class="form-label">Montant (€) *</label>
                                <div class="input-group">
                                    <input type="number" class="form-control" id="amount" name="amount" 
                                           step="0.01" min="0.01" required 
                                           placeholder="Ex: 150.50">
                                    <span class="input-group-text">€</span>
                                </div>
                            </div>
                            
                            <div class="mb-3">
                                <label for="description" class="form-label">Description (optionnel)</label>
                                <textarea class="form-control" id="description" name="description" 
                                          rows="3" placeholder="Ex: Virement pour facture"></textarea>
                            </div>
                            
                            <div class="d-grid gap-2">
                                <button type="submit" class="btn btn-info btn-lg">
                                    <i class="fas fa-check me-2"></i>Effectuer le virement
                                </button>
                                <a href="{{ path('client_transactions_index') }}" class="btn btn-outline-secondary">
                                    <i class="fas fa-times me-2"></i>Annuler
                                </a>
                            </div>
                        </form>
                    {% endif %}
                </div>
            </div>
            
            <div class="card shadow-sm mt-4">
                <div class="card-body">
                    <h5><i class="fas fa-info-circle me-2 text-info"></i>Informations</h5>
                    <ul class="text-muted">
                        <li>Les virements sont traités immédiatement</li>
                        <li>Vérifiez bien le numéro de compte destination</li>
                        <li>Le découvert autorisé est pris en compte</li>
                        <li>Conservez le numéro de transaction pour référence</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}